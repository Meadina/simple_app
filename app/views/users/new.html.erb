<% if current_user && current_user.admin? %>
  <% provide(:title, 'Добавление пользователя') %>
  <% provide(:button_text, 'Добавить нового пользователя') %>
<% else %>
  <% provide(:title, 'Регистрация') %>
  <% provide(:button_text, 'Создать аккаунт') %>
<% end %>

<h1>Регистрация нового пользователя</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
     <%= render 'form' %>
  </div>
</div>
